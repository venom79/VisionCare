{
  "buildCommand": "curl -fsSL https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.22.3-stable.tar.xz | tar xJ && git config --global --add safe.directory \"$PWD/flutter\" && export PATH=\"$PATH:$PWD/flutter/bin\" && flutter config --no-analytics && flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null
}
